mainclass Fibonacci {
    public static void main ( )
    {
        int first, second, i, tmp;
        first= tmp = 0;
        first = first + 1;
        second=1;
        i=0;

        //this is a line comment

        while (ii<10)
        {
            int foo;
            i=i+1;
            tmp=first+second;
            println (tmp);
            /*  this is 
                a block 
                comment 
            */
            first=second;
            second=tmp;
        }
    }
}